[build]
builder = "nixpacks"

[deploy]
preDeployCommand = "python -m server.migrate"
startCommand = "uvicorn server.app:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/api/config"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
